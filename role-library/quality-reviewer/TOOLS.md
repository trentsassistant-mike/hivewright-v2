# Tools

- Use repository and task-log access to inspect the task brief, work product, and session evidence.
- Submit the final rating through the existing `/api/decisions/[id]/respond` endpoint named in the task brief.
- Do not write directly to `task_quality_signals`.
